# <YYYY-MM-DD> — <topic>

> Session evidence record. **Immutable once merged** — a later correction is a *new* handoff that
> links back, never an edit. Filename: `docs/handoffs/<YYYY-MM-DD>-<kebab-topic>.md`.

**Seat:** `<which agent / human, and what access it had>`
**Related:** `<Issue #>` · `<PR #>` · [decision](../DECISIONS.md#<anchor>)

---

## What this session was asked to do

<The deliverable, in one sentence. Not the activity.>

## What was actually done

<Chronological only where order matters for the evidence. Otherwise organize by finding.>

## Findings

For each: the claim, its label, and its evidence path.

- **VERIFIED** — `<claim>` (`path/to/file.py:123`)
- **INFERRED** — `<claim>`, from `<the verified facts>` by `<the reasoning>`
- **ASSUMED** — `<claim>`. **If false:** `<the blast radius>`

## What this does NOT establish

<The most valuable section. Confounds, unreproducible figures, absent mechanisms, seat
limitations. Write against your own interest.>

## Commands run

```bash
# Enough for someone else to reproduce the checks — not a diary of the session.
```

## Open items

<Issues filed, questions for the Owner, work deliberately deferred and why. An item with no
analysis attached is a to-do handed to your successor — answer the cheap ones here.>
